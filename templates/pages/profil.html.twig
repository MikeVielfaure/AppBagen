{% extends "layouts/base.html.twig" %}

{% block body %}  
<div class="container">
<div class="row" style="text-align: center; margin-bottom: 15px">
      <h1 style="color:#337ab7">Profil</h1>
</div>
    
<div class="row" style="position: relative">
    <div class="col-md-6 col-md-offset-3">
        <div class="panel panel-default" style="box-shadow: 0px 0px 10px 0px rgba(26, 26, 26, 0.08);
             -moz-box-shadow: 0px 0px 10px 0px rgba(26, 26, 26, 0.08);
             -webkit-box-shadow: 0px 0px 10px 0px rgba(26, 26, 26, 0.08);">
            <div class="panel-heading">
                <h3 class="panel-title">Mes Informations</h3>
            </div>
            <div class="panel-body" style="padding: 30px">  
                <div class="form-group">  
                    <div class="col-md-12">
                        <div class="col-md-4">
                            <a> Nom : </a>
                        </div>
                        {% if "/modifier/nom" in app.request.pathinfo  %}
                        <form style="padding-left: 0px !important; padding-right:0px !important" method="POST" action="{{ path('profil.valider', {champ:'nom'}) }}" >
                        <div class="col-md-5">
                            <input  type="text" name="nom" value="{{ app.user.nom }}" autofocus >
                        </div>
                        <div class="col-md-3">
                            <button style="height:30px" "type="submit"  >Valider
                            </button>
                        </div>
                        </form>
                        {% else %}
                        <div class="col-md-5">
                            <a> {{ app.user.nom }} </a>
                        </div>
                        <div class="col-md-3">
                            <a href="{{ path('profil.modifier', {champ:'nom'}) }}">modifier</a>
                        </div>
                        {% endif %}
                    </div>
                </div>
                <div class="form-group"> 
                    <div class="col-md-12">
                        <div class="col-md-4">
                            <a> Prénom : </a>
                        </div>
                        {% if "/modifier/prenom" in app.request.pathinfo  %}
                        <form style="padding-left: 0px !important; padding-right:0px !important" method="POST" action="{{ path('profil.valider', {champ:'prenom'}) }}" >
                        <div class="col-md-5">
                            <input  type="text" name="prenom" value="{{ app.user.prenom }}" autofocus >
                        </div>
                        <div class="col-md-3">
                            <button style="height:30px" "type="submit"  >Valider
                            </button>
                        </div>
                        </form>
                        {% else %}
                        <div class="col-md-5">
                            <a> {{ app.user.prenom }} </a>
                        </div>
                        <div class="col-md-3">
                            <a href="{{ path('profil.modifier', {champ:'prenom'}) }}">modifier</a>
                        </div>
                        {% endif %}
                    </div>        
                </div>
                <div class="form-group">
                    <div class="col-md-12">     
                        <div class="col-md-4">
                            <a> Adresse : </a>
                        </div>
                        {% if "/modifier/adresse" in app.request.pathinfo  %}
                        <form style="padding-left: 0px !important; padding-right:0px !important" method="POST" action="{{ path('profil.valider', {champ:'adresse'}) }}" >
                        <div class="col-md-5">
                            <input  type="text" name="adresse" value="{{ app.user.adresse }}" autofocus >
                        </div>
                        <div class="col-md-3">
                            <button style="height:30px" "type="submit"  >Valider
                            </button>
                        </div>
                        </form>
                        {% else %}
                        <div class="col-md-5">
                            <a> {{ app.user.adresse }} </a>
                        </div>
                        <div class="col-md-3">
                            <a href="{{ path('profil.modifier', {champ:'adresse'}) }}">modifier</a>
                        </div>
                        {% endif %}
                    </div>
                </div>
                <div class="form-group">
                    <div class="col-md-12">     
                        <div class="col-md-4">
                            <a> Code postal : </a>
                        </div>
                        {% if "/modifier/codepostal" in app.request.pathinfo  %}
                        <form style="padding-left: 0px !important; padding-right:0px !important" method="POST" action="{{ path('profil.valider', {champ:'codepostal'}) }}" >
                        <div class="col-md-5">
                            <input  type="text" name="codepostal" value="{{ app.user.codepostal }}" autofocus >
                        </div>
                        <div class="col-md-3">
                            <button style="height:30px" "type="submit"  >Valider
                            </button>
                        </div>
                        </form>
                        {% else %}
                        <div class="col-md-5">
                            <a> {{ app.user.codepostal }} </a>
                        </div>
                        <div class="col-md-3">
                            <a href="{{ path('profil.modifier', {champ:'codepostal'}) }}">modifier</a>
                        </div>
                        {% endif %}
                    </div>
                </div>
                <div class="form-group">
                    <div class="col-md-12">     
                        <div class="col-md-4">
                            <a> Ville : </a>
                        </div> 
                        {% if "/modifier/ville" in app.request.pathinfo  %}
                        <form style="padding-left: 0px !important; padding-right:0px !important" method="POST" action="{{ path('profil.valider', {champ:'ville'}) }}" >
                        <div class="col-md-5">
                            <input  type="text" name="ville" value="{{ app.user.ville }}" autofocus >
                        </div>
                        <div class="col-md-3">
                            <button style="height:30px" "type="submit"  >Valider
                            </button>
                        </div>
                        </form>
                        {% else %}
                        <div class="col-md-5">
                            <a> {{ app.user.ville }} </a>
                        </div>
                        <div class="col-md-3">
                            <a href="{{ path('profil.modifier', {champ:'ville'}) }}">modifier</a>
                        </div>
                        {% endif %}
                    </div>
                </div>             
            </div> 
            <div style="text-align:center; margin-bottom: 30px;">
                <a href="{{ path('profil.modifier.tous') }}" class="btn btn-lg btn-primary" >Modifier</a>
            </div>  
        </div>
    </div>
</div>
</div>
{% endblock %}

